# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @backend/ms_product`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t backend-ms_product`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system backend-ms_product && \
          adduser --system -G backend-ms_product backend-ms_product

COPY apps/ms_product/dist backend-ms_product/
COPY apps/ms_product/package.json backend-ms_product/
RUN chown -R backend-ms_product:backend-ms_product .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix backend-ms_product --omit=dev -f install

CMD [ "node", "backend-ms_product" ]
